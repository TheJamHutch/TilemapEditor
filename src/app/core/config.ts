export const config = {
    autosaveEnabled: false,
    storageEnabled: false,
    editor: {
      resolution: { x: 800, y: 600 },
      mapDimensions: { x: 100, y: 100 },
      tileSize: 32,
      lineDashSpeed: 4                    // Controls the speed of the line dash animation shown when selecting tiles.
    },
    palette: {
      resolution: { x: 600, y: 400 },
      cellSize: 32
    }
  };